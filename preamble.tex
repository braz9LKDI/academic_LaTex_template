% ----------------------------- ENCODING AND FONT ---------------------------- %

\usepackage[utf8]{inputenc} % Source file encoding
\usepackage[T1]{fontenc}    % 8-bit fonts: proper accents (ñ, á) and hyphenation
\usepackage{lmodern}        % Modern, scalable version of Computer Modern
\usepackage{microtype}      % Subtle typographical improvements

% --------------------------------- LANGUAGE --------------------------------- %

\usepackage[spanish]{babel}
\usepackage{csquotes}       % Context-sensitive quotes via \enquote{...}

% -------------------------------- PAGE LAYOUT ------------------------------- %

\usepackage{geometry}
\geometry{
    left=1.5cm,
    right=1.5cm,
    top=1.5cm,
    bottom=1.5cm
}
\pagestyle{plain}
\emergencystretch=2em       % Reduces overfull \hbox warnings on long URLs/words

% ----------------------------- MATH AND SCIENCE ----------------------------- %

\usepackage{amsmath}        % Core math
\usepackage{mathtools}      % Extends amsmath and fixes a few quirks
\usepackage{amsfonts}
\usepackage{siunitx}        % Proper SI units: \SI{9.81}{m/s^2}

% ----------------------------- GRAPHICS AND COLOR --------------------------- %

\usepackage{graphicx}       % For images
\usepackage{xcolor}         % For colors
\usepackage{float}          % For float placement [H]
\usepackage{caption}        % For personalizing captions
\graphicspath{{assets/}}

% ---------------------------------- TABLES ---------------------------------- %

\usepackage{multirow}       % Cells spanning multiple rows
\usepackage{tabularx}       % For advanced tables
\usepackage{booktabs}       % For professional tables

% ---------------------------- ALGORITHMS AND CODE --------------------------- %

\usepackage{algorithm}
\usepackage{algpseudocode}

% --------------------------- LINKS AND CROSS-REFS --------------------------- %

\usepackage[
    colorlinks=true,
    linkcolor=blue,
    urlcolor=blue,
    citecolor=red
]{hyperref}                 % For links (load near the end)
\usepackage{cleveref}       % Smarter \cref references (load AFTER hyperref)

% ------------------------------ CUSTOM COMMANDS ----------------------------- %

\newcommand{\norm}[1]{\left \lVert #1 \right \rVert}
\newcommand{\card}[1]{\left \lvert #1 \right \rvert}
\newcommand{\abs}[1]{\left \lvert #1 \right \rvert}

\newcommand{\ceil}[1]{\left \lceil #1 \right \rceil}
\newcommand{\floor}[1]{\left \lfloor #1 \right \rfloor}

\newcommand{\pln}[1]{\ln \left (#1 \right)}
\newcommand{\plog}[2][10]{\log_{ #1 } \left (#2 \right )}

\newcommand{\pprime}[1]{\left (#1 \right )^\prime}

\newcommand{\psin}[1]{\sin \left (#1 \right )}
\newcommand{\pcos}[1]{\cos \left (#1 \right )}
\newcommand{\ptan}[1]{\tan \left (#1 \right )}
\newcommand{\pcsc}[1]{\csc \left (#1 \right )}
\newcommand{\psec}[1]{\sec \left (#1 \right )}
\newcommand{\pcot}[1]{\cot \left (#1 \right )}

\newcommand{\parcsin}[1]{\arcsin \left (#1 \right )}
\newcommand{\parccos}[1]{\arccos \left (#1 \right )}
\newcommand{\parctan}[1]{\arctan \left (#1 \right )}
\DeclareMathOperator{\arcsec}{arcsec}
\newcommand{\parcsec}[1]{\arcsec \left(#1 \right )}
\DeclareMathOperator{\arccsc}{arccsc}
\newcommand{\parccsc}[1]{\arccsc \left(#1 \right )}

\newcommand{\por}[1]{\; \lor \;}
\newcommand{\pand}[1]{\; \land \;}
